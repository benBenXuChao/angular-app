import { MyForOfDirective } from './my-for-of.directive';

describe('MyForOfDirective', () => {
  it('should create an instance', () => {
    const directive = new MyForOfDirective();
    expect(directive).toBeTruthy();
  });
});
