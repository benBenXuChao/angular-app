import { ExportAsDirective } from './directive-exercise.directive';

describe('ExportAsDirective', () => {
  it('should create an instance', () => {
    const directive = new ExportAsDirective();
    expect(directive).toBeTruthy();
  });
});
